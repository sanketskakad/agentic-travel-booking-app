# Environment configuration for Multi-Agent Travel Booking App

# Mock API Base URL
# - When running Python app locally (outside docker): http://localhost:3000
# - When running inside Docker Compose container: http://llm-mock-apis:3000
MOCK_API_BASE_URL=http://localhost:3000
